This is the backend part of my full-stack (backend centered) final project for the Logate Academy course, developed solo.

There were several commits beforehand that I did before that are not actually included in this project due to a security issue that necessitated the destruction of the original repo. 

-Role and User entity created and linked via proxy table. Created first permissions mockups but are unfinished in their current state. TODO: create actual permissions that can be mapped to JSON. Added additional required Jackson dependencies.<br />
-Finished basic relational setup of entities and field initialization. Next Step: initialize permissions for the JSON and start working on the actual DB and REST part next. <br />
-Setup of initial repository interfaces, service classes and rest controllers. Clear division between handlers of duties.